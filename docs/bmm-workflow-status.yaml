# Workflow Status Template
# This tracks progress through BMM methodology phases
# Phase 3/4 (Implementation) is tracked separately in sprint-status.yaml

# generated: 2025-11-22
# project: Baostock
# project_type: software
# selected_track: quick-flow
# field_type: brownfield
# workflow_path: quick-flow-brownfield.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2025-11-22"
project: "Baostock"
project_type: "software"
selected_track: "quick-flow"
field_type: "brownfield"
workflow_path: "quick-flow-brownfield.yaml"

# QUICK FLOW STATUS:
# ==================
# Quick Flow uses tech-spec instead of full PRD workflow
# Implementation is tracked in sprint-status.yaml

workflow_status:
  # Prerequisite: Documentation (if_undocumented)
  document-project: docs/project-analysis/bmm-index.md

  # Phase 0: Discovery (Optional)
  brainstorm-project: optional
  research: optional

  # Phase 1: Quick Flow Planning (Tech Spec Focus)
  # Quick Flow skips full PRD, uses existing product documentation
  prd: skipped  # Quick Flow uses existing docs, not full PRD workflow
  validate-prd: skipped
  create-design: skipped  # Uses existing UX design if available

  # Phase 2: Solutioning (Simplified for Quick Flow)
  tech-spec: docs/epics.md  # Quick Flow uses tech-spec/epics instead
  create-architecture: docs/architecture.md  # Architecture still needed
  validate-architecture: optional
  solutioning-gate-check: docs/implementation-readiness-assessment-2025-11-07.md

  # Phase 3: Implementation (Tracked in sprint-status.yaml)
  sprint-planning: docs/sprint-status.yaml
  # Story implementation status tracked in sprint-status.yaml
  # Epic R (Architecture Refactoring) - Completed ✅
  epic-r: done
  story-r0-architecture-blueprint: done
  story-r1-state-unification: done
  story-r2-service-refactor: done
  story-r3-data-cleanup: done
  story-r4-component-refactor: done
  story-r5-di-refactor: done

  # Legacy Epics (Completed)
  epic-1: done
  epic-2: done